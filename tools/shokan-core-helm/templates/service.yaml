apiVersion: v1
kind: Service
metadata:
  name: {{ include "shokan-core.fullname" . }}
  labels:
    {{- include "shokan-core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "shokan-core.selectorLabels" . | nindent 4 }}
